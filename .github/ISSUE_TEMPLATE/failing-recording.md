---
name: A recording that does not replay
about: The suite came out, and running it does not do what the recording did
labels: recording
---

<!-- The two things that make this reproducible in minutes rather than days.
     Everything else is optional. -->

## The suite

<!-- Paste the whole .robot file, or attach it. The locators and the waits are
     the evidence; a screenshot of them is not. -->

```robot

```

## The page

<!-- The URL, if it is public. If it is not, say what the element was — a
     combobox, a grid row, an editor — and what the markup around it looks
     like. "A React table with virtualised rows" is enough to start. -->

## What happened when you ran it

<!-- Paste the failure as Robot printed it, including the keyword name.
     "Element 'id:x' not visible after 10 seconds" says something quite
     different from "element click intercepted". -->

```

```

## Anything else

- Chrome version:
- Operating system:
- Zelector version (the popup shows it):
